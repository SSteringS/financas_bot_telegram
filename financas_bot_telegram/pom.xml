<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.4.5</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>br.com.satyan.stering.saita</groupId>
	<artifactId>financas-bot-telegram</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>financas_bot_telegram</name>
	<description>financas_bot_telegram</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<pitest.version>1.25.9</pitest.version>
		<pitest-junit5-plugin.version>1.2.3</pitest-junit5-plugin.version>
		<maven-pmd-plugin.version>3.26.0</maven-pmd-plugin.version>
		<!--
			Escopo do PMD. O mojo `includeTests` NAO tem user property propria: passar
			-Dpmd.includeTests=true direto e silenciosamente ignorado, e o run devolve o
			numero de producao com cara de numero de teste. Amarrar a esta property de
			projeto e o que faz o override de linha de comando funcionar de verdade.
		-->
		<pmd.includeTests>false</pmd.includeTests>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>io.awspring.cloud</groupId>
				<artifactId>spring-cloud-aws-dependencies</artifactId>
				<version>3.2.0</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>

		<dependency>
			<groupId>org.telegram</groupId>
			<artifactId>telegrambots-meta</artifactId>
			<version>6.9.7.1</version>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>1.18.32</version>
			<optional>true</optional>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.7.0</version>
		</dependency>
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>2.15.1</version>
		</dependency>

		<dependency>
			<groupId>org.json</groupId>
			<artifactId>json</artifactId>
			<version>20230227</version>
		</dependency>

		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-mysql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<dependency>
			<groupId>io.awspring.cloud</groupId>
			<artifactId>spring-cloud-aws-starter-s3</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>


<dependency>
			<groupId>io.awspring.cloud</groupId>
			<artifactId>spring-cloud-aws-starter-secrets-manager</artifactId>
		</dependency>

		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.6</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>1.20.4</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>mysql</artifactId>
			<version>1.20.4</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-cloudwatch2</artifactId>
		</dependency>

	</dependencies>

	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>1.18.32</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!--
				Mutation testing (PIT). Piloto de escopo reduzido: quatro classes de logica pura,
				sem contexto Spring. Nao esta ligado a nenhuma fase do build — roda so sob demanda:
				  mvn org.pitest:pitest-maven:mutationCoverage -f financas_bot_telegram/pom.xml

				excludedTestClasses NAO e otimizacao: os *IntegrationTest sobem MySQL real via
				Testcontainers, e o PIT reexecuta a suite que cobre cada mutante uma vez por mutante.
				Com container no caminho o run nao termina.
			-->
			<plugin>
				<groupId>org.pitest</groupId>
				<artifactId>pitest-maven</artifactId>
				<version>${pitest.version}</version>
				<dependencies>
					<!-- Obrigatorio: sem ele o PIT nao reconhece testes JUnit 5. -->
					<dependency>
						<groupId>org.pitest</groupId>
						<artifactId>pitest-junit5-plugin</artifactId>
						<version>${pitest-junit5-plugin.version}</version>
					</dependency>
				</dependencies>
				<configuration>
					<targetClasses>
						<param>br.com.satyan.stering.saita.financasbottelegram.domain.service.LegendaParser</param>
						<param>br.com.satyan.stering.saita.financasbottelegram.application.strategy.PaymentRequestStrategy</param>
						<param>br.com.satyan.stering.saita.financasbottelegram.application.strategy.PaymentProofStrategy</param>
						<param>br.com.satyan.stering.saita.financasbottelegram.adapters.in.whatsapp.security.MetaSignatureValidator</param>
					</targetClasses>
					<excludedTestClasses>
						<param>*IntegrationTest</param>
					</excludedTestClasses>
					<outputFormats>
						<param>XML</param>
						<param>HTML</param>
					</outputFormats>
					<timestampedReports>false</timestampedReports>
				</configuration>
			</plugin>

			<!--
				Analise estatica (PMD). Ruleset curado em pmd-ruleset.xml — cada regra ativa
				tem justificativa escrita la; o arquivo, nao este bloco, e o entregavel.

				NAO esta ligado a nenhuma fase do build (mesmo padrao do pitest acima).
				Roda so sob demanda:
				  ./mvnw pmd:pmd -f financas_bot_telegram/pom.xml

				O motivo de nao amarrar a `verify` nao e conveniencia: enquanto o experimento
				de alocacao de modelo por papel estiver rodando, build que quebra por violacao
				faz o agente otimizar para o linter, o que contamina justamente a variavel
				medida (Q7 = violacoes de ruleset). Relatorio sim, gate nao.

				O goal `pmd:check` — o que falha o build — NAO esta configurado de proposito.
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-pmd-plugin</artifactId>
				<version>${maven-pmd-plugin.version}</version>
				<configuration>
					<rulesets>
						<ruleset>${project.basedir}/pmd-ruleset.xml</ruleset>
					</rulesets>
					<!-- HTML para leitura humana; o XML (target/pmd.xml) sai sempre, e o parseavel. -->
					<format>html</format>
					<!-- Sem jxr no projeto: link para fonte cruzado quebraria o relatorio. -->
					<linkXRef>false</linkXRef>
					<!-- Java 21: mesma release do compiler. Parser precisa aceitar a sintaxe do projeto. -->
					<targetJdk>${java.version}</targetJdk>
					<!--
						Producao por default. O numero que inclui teste sai de um segundo run com
						-Dpmd.includeTests=true — o item #7 do backlog exige Q7_producao e
						Q7_teste separados, e um total unico dilui um no outro.
						Atencao: o run com a flag devolve producao + teste; Q7_teste e a diferenca
						entre os dois runs, nao a saida direta de um deles.
					-->
					<includeTests>${pmd.includeTests}</includeTests>
					<!--
						Explicito de proposito, apesar de 5 ja ser o default do plugin: deixa
						registrado que NAO ha filtro por prioridade. A curadoria e no ruleset,
						regra a regra e com justificativa — filtrar por numero aqui esconderia
						regra sem discutir regra, e em silencio.
					-->
					<minimumPriority>5</minimumPriority>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
