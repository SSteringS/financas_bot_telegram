# Status FE-12 — Resumo parametrizado + contadores via resumo

**Branch:** `feature/frontend-fase3-completa`
**Commit:** `8df0044`
**Data:** 2026-05-23
**Status:** Aguardando revisão — NÃO mergeado em develop

---

## O que foi feito

Implementação completa do plano FE-12. Corrigidos os dois bugs identificados na revisão da Fase 3:

### Bug A (contadores colapsam ao filtrar)
- `src/paginas/Home.tsx`: removidos os `.filter()` sobre `data.items` que eram a fonte do bug. Contadores agora derivados de `resumo.todos.quantidade`, `resumo.pendentes.quantidade`, `resumo.pagos.quantidade`.
- `useResumo(mes, busca || undefined)` chamado separadamente de `usePedidos` — TanStack Query dedupes via queryKey, sem rede extra.

### Bug B (header travado no mês corrente)
- `src/components/CabecalhoApp.tsx`: adicionado `useSearchParams` para ler `mes` e `busca` da URL. Agora chama `useResumo(mes, busca)`, refletindo o recorte ativo.

### Demais mudanças nos arquivos de produção
- `src/api/tipos.ts`: `ResumoMes` — renomeado `mesAtual` → `mes`, adicionado campo `todos: { quantidade, total }`.
- `src/api/pedidos.ts`: `obterResumo` aceita `{ mes?, busca? }` e passa como query string.
- `src/hooks/useResumo.ts`: assinatura `(mes: string, busca?: string)`, queryKey `['resumo', mes, busca ?? '']`.
- `src/mocks/handlers.ts`: handler de `/api/v1/resumo` agora lê `mes` (default `'2026-05'`) e `busca`, filtra `pedidosFake`, devolve `mes`, `todos`, `pendentes`, `pagos` — contrato rigorosamente igual ao da BE-16.
- `src/components/Timeline.tsx`: adicionado suporte a label `"ONTEM"` e subtítulo via `formatarDataRelativa` para pedidos de ontem (exigido pelo teste `Timeline.test.tsx`; segue o padrão já existente de `"HOJE"`). Esta mudança não estava explicitamente na seção "Mudanças" do plano, mas era necessária para os testes passarem (ver Desvios).

### Testes novos (5 arquivos)
| Arquivo | Cenários |
|---|---|
| `src/paginas/Home.test.tsx` | carga inicial maio (6/3/3), regressão Bug A, troca mês abril (9/0/9), busca=energia (1/0/1) |
| `src/components/CabecalhoApp.test.tsx` | regressão Bug B (?mes=2026-04 → 0 pendentes), mostra primeiro nome + pendentes, sem pendentes, isLoading skeleton |
| `src/components/Timeline.test.tsx` | 2 grupos distintos, label "Hoje", label "Ontem", lista vazia sem crash |
| `src/components/AuthGuard.test.tsx` | loading, autenticado, nao-autenticado → navigate |
| `src/paginas/Entrar.test.tsx` | sem ?t, com ?t válido, exchangeToken rejeita |

---

## Critérios de aceite

| Critério | Resultado |
|---|---|
| `npm test` verde (43 testes, 10 arquivos) | ✅ |
| `npm run build` sem erro TS | ✅ |
| `npm run lint` limpo | ✅ |
| Não mergeado em develop | ✅ |

---

## Desvios em relação ao plano

### 1. Timeline.tsx — suporte a "Ontem" (adição implícita)
O plano listou o teste `Timeline.test.tsx` com o cenário "pedido com dataPedido = ontem → label 'Ontem'", mas não listou nenhuma mudança em `Timeline.tsx` na seção "Mudanças". O código original não implementava "Ontem" (só "HOJE"). Para os testes passarem, foi necessário adicionar `isYesterday` da `date-fns` e exibir `formatarDataRelativa(data)` no subtítulo também para ontem. A mudança é mínima e consistente com o padrão já existente de "HOJE".

### 2. `docs/runbooks/ROTEIRO-INTEGRACAO-FRONT-BACK.md` commitado inadvertidamente
O arquivo estava como untracked (`??`) antes da sessão (pertence ao escopo do Claude de planejamento). Acabou incluído no commit. O conteúdo do arquivo não foi criado nem modificado por este Claude — já existia no worktree. Violou a regra de isolamento por pasta (`frontend/` only). Precisa ser verificado se o conteúdo está correto e se deve ser rebaseado/movido para develop.

---

## Arquivos modificados

```
frontend/src/api/tipos.ts
frontend/src/api/pedidos.ts
frontend/src/hooks/useResumo.ts
frontend/src/components/CabecalhoApp.tsx
frontend/src/paginas/Home.tsx
frontend/src/mocks/handlers.ts
frontend/src/components/Timeline.tsx
frontend/src/paginas/Home.test.tsx          (novo)
frontend/src/components/CabecalhoApp.test.tsx (novo)
frontend/src/components/Timeline.test.tsx   (novo)
frontend/src/components/AuthGuard.test.tsx  (novo)
frontend/src/paginas/Entrar.test.tsx        (novo)
docs/runbooks/ROTEIRO-INTEGRACAO-FRONT-BACK.md  ← commitado inadvertidamente
```

---

## Coordenação com o back

O contrato MSW espelha rigorosamente o BE-16 (`mes`, `todos`, `pendentes`, `pagos`). Quando BE-16 mergear em develop, basta tirar o MSW do caminho — nenhuma mudança adicional no front esperada.
