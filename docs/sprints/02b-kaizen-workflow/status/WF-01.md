---
task: WF-01
titulo: "Investigar e mitigar truncamento de writes do Cowork"
data: 2026-05-30
branch: develop
responsavel: claude-plan
estado: concluido
gates:
  build: na
  lint: na
  testes: na
  testes_total: na
  testes_novos: na
  branch_convencao: na
  territorio: ok
commits:
  - pendente
pr: null
desvios: 0
pendencias_humano: 1
---

# WF-01 — Investigar e mitigar truncamento de writes do Cowork

## O que foi feito

Diagnóstico do bug de truncamento das tools `Write`/`Edit` do Cowork (que vinha sendo atribuído a "sync gremlin do OneDrive" — hipótese descartada porque `C:\Users\satya\src\` não está sob OneDrive). Resultado:

- **Criado `docs/aprendizado/cowork-write-truncamento.md`** (107 linhas) — contexto, resumo destilado, pontos-chave, **3 seções de evidência (A/B/C)** com casos vividos na sessão de 2026-05-30 + tabela de workarounds + **bug report rascunho em inglês** (anexo, autocontido, ~300 palavras pronto pro humano colar no feedback do Cowork) + tópicos pra aprofundar.
- **`docs/aprendizado/README.md`** indexa o arquivo novo numa categoria nova "Ferramental / ambiente" (criada nesta task).
- **`docs/roles/planner.md`** ganhou seção "Escrita defensiva de arquivos (workaround Cowork)" com 3 regras operacionais (bash heredoc em writes importantes, `wc -l && tail` pra verificar, terminal Windows do humano como fonte da verdade do estado do repo) + item novo no Checklist do papel + entrada nova em "Ler sempre".
- **`CLAUDE.md`** corrigido — removida menção a "sync gremlin do OneDrive" em §Worktrees git e adicionado bullet apontando pra `docs/aprendizado/cowork-write-truncamento.md` e `docs/roles/planner.md` §Escrita defensiva.

## Desvios do plano

Nenhum.

> **Meta-validação:** durante esta própria task, o bug que está sendo documentado **ocorreu** — o `Edit` em `CLAUDE.md` retornou "updated successfully" mas truncou o final do arquivo (cortou em "- **RDS:** MyS"; perdeu 4 linhas de infraestrutura). A regra defensiva pegou imediatamente: `wc -l && git diff` mostrou o sumiço, e o arquivo foi reescrito via bash heredoc. Caso vivido extra reforça a regra que esta task instaura.

## Decisões tomadas durante a execução

- Categoria nova "Ferramental / ambiente" no `aprendizado/README.md` (em vez de empurrar pra "Ferramentas / fluxo" existente, que está focada em git/CRLF). Sinaliza que o bug é do ambiente Cowork, não do fluxo de git.
- A regra "writes em arquivos importantes" lista paths explícitos (`docs/architecture/`, `docs/decisions/`, `docs/plans/`, `docs/sprints/<NN>/`, `docs/retrospectivas/`, `docs/roles/`, `docs/aprendizado/`) — preferi enumerar a deixar genérico, pra não ter ambiguidade sobre o que é "importante".
- Threshold do `Write`/`Edit` ficou em "patch < 20 linhas **e** arquivo total < 100 linhas" (ambas condições). O `e` é mais restritivo que o `ou` — escolha conservadora; ajustar se ficar incômodo na retro.
- Bug report rascunho ficou em inglês (Cowork é internacional) e foi posto como apêndice do mesmo arquivo de aprendizado, pra evitar criar um sub-arquivo só pra ele.

## Decisões pendentes (esperando humano)

Nenhuma sobre conteúdo. **Pendência única:** o humano precisa rodar `git push` no terminal Windows após revisar `git show HEAD`. (Por isso `pendencias_humano: 1`.)

## Próximos passos / observações pro próximo

- WF-02 vai mexer em `CLAUDE.md` de novo (atualizar refs `docs/status/` → `docs/sprints/<NN>/status/` + ref ao template novo). Espaço de colisão é pequeno — WF-01 só tocou em uma linha do §Worktrees git, WF-02 vai mexer em outras menções.
- WF-03 não toca `CLAUDE.md`, fica fora desse cone.
- Próximas sessões do planner devem ler `docs/roles/planner.md` no boot — a regra 1 (bash heredoc) é o que pega o bug mais frequente.

## Arquivos criados/modificados

- `docs/aprendizado/cowork-write-truncamento.md` (novo: 107 linhas; aprendizado + evidência A/B/C + bug report rascunho)
- `docs/aprendizado/README.md` (modificado: +categoria "Ferramental / ambiente" + 1 entrada)
- `docs/roles/planner.md` (modificado: +seção "Escrita defensiva" com 3 regras + item de checklist + entrada em "Ler sempre")
- `CLAUDE.md` (modificado: §Worktrees git — removida menção a "sync gremlin do OneDrive" + bullet novo apontando pra aprendizado e planner.md)
- `docs/sprints/02b-kaizen-workflow/status/WF-01.md` (novo: este arquivo)
